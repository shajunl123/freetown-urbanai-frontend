# Frontend runtime configuration
#
# The React/Vite app talks only to the backend API. It should not call n8n
# or any model/workflow webhook directly.
VITE_POLICY_API_BASE_URL=http://localhost:3001/api
